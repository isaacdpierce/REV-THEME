<div class="article">

    <div class="page-header cf">
      <h1 class="majortitle">{{ page.title }}</h1>
    </div>
    {% section 'gdc-cards-transparency' %}               
    {% if page.content != blank %}
      <div class="container cf user-content ">
        {{ page.content }}
      </div>
    {% endif %}

</div>
